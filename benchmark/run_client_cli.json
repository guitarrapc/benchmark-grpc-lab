{
  "Parameters": {
    "commands": [
      "#!/bin/bash",
      "export DOTNET_CLI_HOME=/tmp",
      "sudo killall Benchmark.Client || true",
      "dotnet --version",
      "BENCHCLIENT_RUNASWEB=false ~/client/Benchmark.Client benchmarkrunner runallclient -processcount 5 -iteations '256,1024,4096,16384' -benchcommand benchall -hostAddress http://localhost:80"
    ]
  }
}
